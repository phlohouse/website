# Codebase Audit Findings (2026-02-13)

## Scope

- Repo-wide checklist run across `src/`, `packages/`, `tests/`, `docs/`, `.github/workflows/`.
- Quality gates attempted: `make check`, `ruff`, `ty`, `pytest` (non-integration + integration subsets).
- Environment note: local `uv` version is `0.4.18`.

## Gate Results Snapshot

- `uv run ruff check .` (before env drift): pass.
- `uv run ruff format --check .` (before env drift): pass.
- `uv run pytest -m "not integration"`: `590 passed`, `2 failed` (`tests/test_cli_workflow.py`).
- `uv run pytest -m integration`: `264 passed`, `51 skipped`, `10 failed`.
- `make check`: fails immediately (`uv run --script` unsupported in local uv).

## Findings (Severity Ordered)

### 1) High: Local quality gate cannot run with current uv compatibility

- Evidence:
  - `scripts/run-parallel:1` uses shebang `#!/usr/bin/env -S uv run --script`.
  - `Makefile:8` invokes `scripts/run-parallel` for `check`.
  - Runtime failure: `error: unexpected argument '--script' found`.
- Impact:
  - `make check` is non-functional for environments with uv versions lacking `uv run --script`.
- Fix direction:
  - Pin minimum uv version in docs/tool bootstrap, or replace shebang pattern with a portable runner.

### 2) High: CLI contract drift for `phlo workflow create`

- Evidence:
  - Tests expect command exists: `tests/test_cli_workflow.py:8`, `tests/test_cli_workflow.py:16`.
  - CLI root does not register workflow command: `src/phlo/cli/main.py:35`, `src/phlo/cli/main.py:36`, `src/phlo/cli/main.py:37`, `src/phlo/cli/main.py:38`.
  - User-facing docs advertise command: `docs/reference/cli-reference.md:722`, `docs/getting-started/installation.md:159`, `docs/operations/testing.md:534`.
  - Runtime: `Error: No such command 'workflow'.`
- Impact:
  - Broken documented workflow onboarding path.
- Fix direction:
  - Ensure workflow command is always registered or clearly gate docs/tests behind plugin availability.

### 3) High: Orchestrator selection hard-fails when no adapter registered

- Evidence:
  - `src/phlo/orchestrators/selection.py:16` plugin discovery + `src/phlo/orchestrators/selection.py:20` hard error.
  - Integration failures in workflow discovery tests: `tests/test_framework_integration.py:26`, `tests/test_framework_integration.py:79`.
- Impact:
  - Workflow discovery path fails instead of graceful fallback/skip when orchestrator plugin unavailable.
- Fix direction:
  - Add deterministic fallback behavior for tests/dev mode or tighten install contract for required orchestrator package.

### 4) High: Test suite contains stale patch targets after Trino resource refactor

- Evidence:
  - Tests patch `phlo_trino.resource.config`: `packages/phlo-iceberg/tests/test_resources.py:68`, `packages/phlo-iceberg/tests/test_resources.py:181`, `packages/phlo-trino/tests/test_integration_trino.py:158`, `packages/phlo-trino/tests/test_integration_trino.py:169`.
  - Module now uses `_trino_settings` / `_iceberg_settings`: `packages/phlo-trino/src/phlo_trino/resource.py:15`, `packages/phlo-trino/src/phlo_trino/resource.py:16`, `packages/phlo-trino/src/phlo_trino/resource.py:28`, `packages/phlo-trino/src/phlo_trino/resource.py:29`.
  - Runtime failure: `AttributeError ... does not have the attribute 'config'`.
- Impact:
  - False-negative integration/unit results; reduced trust in CI signal.
- Fix direction:
  - Update tests to patch new settings access points or inject config dependency explicitly.

### 5) Medium: Integration test assumptions not robust (service/dependency availability)

- Evidence:
  - `packages/phlo-iceberg/tests/scripts/test_duckdb_iceberg.py:73` hard-fails on connection refused to Nessie instead of skipping.
  - `packages/phlo-dbt/tests/test_integration_dbt.py:133` and `packages/phlo-dbt/tests/test_integration_dbt.py:160` fail on missing `dbt.adapters.duckdb`.
  - `packages/phlo-grafana/tests/test_integration_infrastructure.py:151` expects docker-compose-style schema keys not present in current plugin output.
  - Plugin returns service metadata format: `packages/phlo-postgrest/src/phlo_postgrest/plugin.py:27`.
- Impact:
  - Unstable integration suite in common local setups.
- Fix direction:
  - Standardize skip guards and normalize service-definition contract checks.

### 6) Medium: Type-checking scope mismatch between local and CI

- Evidence:
  - CI type-checks core only: `.github/workflows/ci.yml:54` (`ty check src/phlo`).
  - Local check target runs `uv run ty check` (full scope): `Makefile:11`.
  - Full ty run previously flagged unresolved imports in test files (`conftest.py:105`, `conftest.py:106`, `packages/phlo-minio/tests/test_integration_minio.py:224`).
- Impact:
  - CI may miss type regressions outside `src/phlo`.
- Fix direction:
  - Align CI and local type-check scope and dependency setup.

### 7) Medium: uv command usage mismatch in workflows vs local tooling

- Evidence:
  - CI uses `uv sync --dev --group dev`: `.github/workflows/ci.yml:74`, `.github/workflows/ci.yml:104`.
  - Local uv (`0.4.18`) rejects `--group`.
- Impact:
  - Local reproduction of CI setup is blocked on uv version mismatch.
- Fix direction:
  - Document minimum uv version explicitly and add bootstrap check.

### 8) Low: Package completeness gap in `phlo-observatory-example`

- Evidence:
  - Missing `README.md` and `tests/` in `packages/phlo-observatory-example`.
- Impact:
  - Reduced discoverability and confidence for example package behavior.
- Fix direction:
  - Add minimal README + smoke test.

### 9) Low: Dependency constraints mostly unbounded

- Evidence:
  - Repeated uv warnings for missing lower bounds in `pyproject.toml` and package manifests.
  - Example unbounded entries: `pyproject.toml:30`, `pyproject.toml:31`, `pyproject.toml:32`, `pyproject.toml:33`, `pyproject.toml:34`, `pyproject.toml:35`, `pyproject.toml:36`.
- Impact:
  - Higher risk of nondeterministic upgrades and breakage.
- Fix direction:
  - Add explicit lower bounds for all runtime dependencies.

## Additional Notes

- Hygiene scan found no tracked `.DS_Store`/`__pycache__`/`*.pyc` files.
- Package inventory found one explicit documentation/test gap:
  - `packages/phlo-observatory-example` (no README, no tests).
